#include<string.h> 
using namespace std;
//十进制转二进制
int main(int argc,char * argv[])
{
	int num,i=0;
	int a[32]={0};
	cin>>num;
	do{
		a[i++]=num%2;
		num/=2;
	}while(num!=0);
    for(--i;i>=0;--i)
		cout<<a[i];
	cout<<endl;
	return 0;
}
